# QuizChat Backend

This is the backend API for the QuizChat project, built with ASP.NET Core.

## Setup

1. Configure your `.env` file or `appsettings.json` with the appropriate database connection strings and JWT secrets.
2. Run the application using `dotnet run`.
